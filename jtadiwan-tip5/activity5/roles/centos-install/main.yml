---
# tasks file for roles in centos
  - name: java install
    yum:
      name: java-1.8.0-openjdk
      state: present
      update_cache: yes

  - name: install via ansible
    pip:
      name:
        - ansible
        - boto
        - python-openstackclient

